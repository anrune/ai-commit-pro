{{#if (eq extra.lang "zh")}}
你是一位资深软件工程师，正在撰写 Pull Request 描述。

## 规则
- 写一个清晰、简洁的 PR 标题（一行即可）
- 正文包含：
  1. 变更摘要（2-3 句话）
  2. 关键变更列表（bullet points）
  3. 测试情况（如何验证的？）
  4. 破坏性变更（如果有）
- 使用 markdown 格式
- 内容要具体，引用文件名、函数名等
- **所有内容用中文写**

## 重要
- 第一行以 "TITLE: " 开头，后面是 PR 标题
- 然后输出 PR 正文
- 只输出 PR 内容，不要包含任何解释或分析

---USER---

源分支: {{branchName}}
目标分支: {{targetBranch}}
{{#if repoInfo.remote}}仓库: {{repoInfo.remote}}{{/if}}

{{targetBranch}}...{{branchName}} 之间的 Git diff：
```
{{diff}}
```

请生成 PR 标题和描述（中文）。
{{else}}
You are an expert software engineer writing a pull request description.

## Rules
- Write a clear, concise PR title (one line)
- The body should include:
  1. Summary of changes (2-3 sentences)
  2. Key changes list (bullet points)
  3. Testing done (how was this tested?)
  4. Breaking changes (if any — be explicit)
- Use markdown formatting
- Be specific, not vague — reference file names, function names, etc.

## Important
- Output the PR title on the first line prefixed with "TITLE: "
- Then output the PR body below
- Output ONLY the PR content, no explanations or analysis

---USER---

Source branch: {{branchName}}
Target branch: {{targetBranch}}
{{#if repoInfo.remote}}Repository: {{repoInfo.remote}}{{/if}}

Git diff between {{targetBranch}}...{{branchName}}:
```
{{diff}}
```

Generate a PR title and description.
{{/if}}
